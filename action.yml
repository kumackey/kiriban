name: 'Kiriban Action'
description: 'This action checks if the number is kiriban.'
author: 'kumackey <kumak1t09e0@gmail.com>'
runs:
  using: "composite"
  steps:
    - run: go run cmd/main.go
      shell: bash
      with:
        events: ${{ github.event.pull_request.number }}
branding:
  icon: 'list'
  color: 'gray-dark'